Яркин Максим



double a, b, c; // Объявляем 3 переменные типа double
   cout << "Введите значение:"; // Запрашиваем у пользователя значение
   cin >> a; // записываем значение в переменную a
   cout << "Введите значение:"; // запрашиваем у пользователя значение
   cin >> b; // записываем в переменную b
   double* A(&a), * B(&b), * C(&c); // создаем три указателя
   *A *= 3; // умножаем значение 3
   *C = *A; // С становится а
   *A = *B; // A становится B
   *B = *C; // B Становится C 
   cout << *A << endl; // Выводим значение переменной A
   cout << *B;\ // Выводим значение переменной B
